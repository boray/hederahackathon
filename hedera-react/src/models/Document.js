class Document {

    constructor(name, uri, documentHash, timestamp) {
        this.name = name;
        this.uri = uri;
        this.documentHash = documentHash;
        this.timestamp = timestamp;
    }
}

export default Document;